# Midlife Dave audio (deprecated)

Midlife Divorced Dave speaks only through the runtime API pipeline (`VoiceDirector` → `character-speech`).

Do not place prerecorded opponent dialogue files here.
