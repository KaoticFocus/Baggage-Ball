[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev:game"
  targetPort = 5173
  port = 8888

[functions]
  node_bundler = "esbuild"
