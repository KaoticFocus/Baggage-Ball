/**
 * @deprecated Import from VoiceDirector instead.
 * Kept as a thin re-export so older paths resolve to the single VoiceDirector.
 */
export {
  voiceDirector as speechDirector,
  voiceDirector,
  createSpeechRequestId,
  VoiceDirector,
} from './VoiceDirector';
